{/* Starter skeleton: to be rewritten. It doubles as a test bed for `mdx-components.tsx`. */}

Paragraphe d'introduction, avec du **gras**, de l'_italique_, du `code inline` et un [lien](https://nextjs.org).

## Un titre de section

Un paragraphe de corps de texte, assez long pour vérifier l'interligne, la largeur de lecture et la
couleur du texte secondaire une fois le style appliqué.

### Un sous-titre

- Premier élément de liste
- Deuxième élément de liste
- Troisième élément de liste

1. Première étape
2. Deuxième étape

> Une citation, pour vérifier son traitement visuel.

```tsx
export default async function Page() {
  const articles = getArticles('fr')

  return <ArticleList articles={articles} />
}
```

## Une dernière section

Un paragraphe de conclusion.
