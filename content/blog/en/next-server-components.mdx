{/* Starter skeleton: to be rewritten. It doubles as a test bed for `mdx-components.tsx`. */}

Introduction paragraph, with **bold**, _italic_, `inline code` and a [link](https://nextjs.org).

## A section heading

A body paragraph, long enough to check line height, reading width and secondary text color once the
styles are applied.

### A sub heading

- First list item
- Second list item
- Third list item

1. First step
2. Second step

> A quote, to check how it is rendered.

```tsx
export default async function Page() {
  const articles = getArticles('en')

  return <ArticleList articles={articles} />
}
```

## A closing section

A closing paragraph.
